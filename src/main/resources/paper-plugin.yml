name: ${project.name}
main: ${project.groupId.refactored}.${project.artifactId}.${project.name}
version: ${brotkrumen.version}
api-version: '${minecraft.api.version}'
description: ${project.description}
website: 'https://github.com/RolePlay-Cauldron/Brotkrumen'
authors: [ Lorias-Jak, Velyn-N ]
